{
  "name": "foundationsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dskydiver/foundationsubgraph",
    "create-local": "graph create --node http://localhost:8020/ dskydiver/foundationsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dskydiver/foundationsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dskydiver/foundationsubgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
